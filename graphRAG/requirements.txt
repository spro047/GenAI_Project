requests>=2.28.0
networkx>=3.0
python-dotenv>=1.0.0
flask>=3.0.0
# llama-cpp-python>=0.3.0  # (Optional: Only needed for local GGUF models, requires C++ Build Tools)
huggingface_hub
google-search-results
fpdf2
# chromadb>=1.5.0  # (Optional: Only needed for RAG/vector search features, heavy deps)
# numpy<2.0.0       # (Required by chromadb, uncomment if chromadb is enabled)
